# Pipelines are made of one or more jobs and may include resources and variables.
# Jobs are made of one or more steps plus some job-specific data. Steps can be
# tasks, scripts, or references to external templates.

jobs:
  - job: Linux
    pool:
      vmImage: 'ubuntu-16.04'
    strategy:
      maxParallel: 2
      matrix: 
        py36:
          PY_VERSION: 3.6
        py27:
          PY_VERSION: 2.7
    steps:
      - task: CondaEnvironment@1
        inputs:
          createCustomEnvironment: true
          environmentName: 'vigra'
          installOptions: '--prefix $(AGENT_WORKFOLDER)/conda'
          createOptions: '--prefix $(AGENT_WORKFOLDER)/conda'
          packageSpecs: 'python=$(PY_VERSION)'
          updateConda: true
      - script: |
          echo "Will start the conda install of prerequisites" || exit 1
          conda install --quiet --yes --mkdir --channel conda-forge jpeg libpng libtiff hdf5 fftw boost boost-cpp numpy nose || exit 1
          printenv || exit 1
          mkdir build || exit 1
          cd build || exit 1
          echo "Will start the cmake configuration" || exit 1
          cmake .. -DTEST_VIGRANUMPY=1 -DDEPENDENCY_SEARCH_PREFIX="$CONDA_PREFIX/Library" -DCMAKE_PREFIX_PATH="$CONDA_PREFIX/Library" || exit 1
          echo "Will start the cmake based build" || exit 1
          cmake --build . --target check --config Release || exit 1
          echo "Completed script successfully" || exit 1
  - job: macOS
    pool:
      vmImage: 'xcode9-macos10.13'
    strategy:
      maxParallel: 2
      matrix: 
        py36:
          PY_VERSION: 3.6
        py27:
          PY_VERSION: 2.7
    steps:
      - task: CondaEnvironment@1
        inputs:
          createCustomEnvironment: true
          environmentName: 'vigra'
          installOptions: '--prefix $(AGENT_WORKFOLDER)/conda'
          createOptions: '--prefix $(AGENT_WORKFOLDER)/conda'
          packageSpecs: 'python=$(PY_VERSION)'
          updateConda: true
      - script: |
          echo "Will start the conda install of prerequisites" || exit 1
          conda install --quiet --yes --mkdir --channel conda-forge jpeg libpng libtiff hdf5 fftw boost boost-cpp numpy nose || exit 1
          printenv || exit 1
          mkdir build || exit 1
          cd build || exit 1
          echo "Will start the cmake configuration" || exit 1
          cmake .. -DTEST_VIGRANUMPY=1 -DDEPENDENCY_SEARCH_PREFIX="$CONDA_PREFIX/Library" -DCMAKE_PREFIX_PATH="$CONDA_PREFIX/Library" || exit 1
          echo "Will start the cmake based build" || exit 1
          cmake --build . --target check --config Release || exit 1
          echo "Completed script successfully" || exit 1
  - job: Windows
    pool:
      vmImage: 'vs2017-win2016'
    strategy:
      maxParallel: 2
      matrix: 
        py36:
          PY_VERSION: 3.6
        py27:
          PY_VERSION: 2.7
    steps:
      - task: CondaEnvironment@1
        inputs:
          createCustomEnvironment: true
          environmentName: 'vigra'
          installOptions: '--prefix $(AGENT_WORKFOLDER)\conda'
          createOptions: '--prefix $(AGENT_WORKFOLDER)\conda'
          packageSpecs: 'python=$(PY_VERSION)'
          updateConda: true
      - script: |
          echo "Will start the conda install of prerequisites" || exit 1
          conda install --quiet --yes --mkdir --channel conda-forge jpeg libpng libtiff hdf5 fftw boost boost-cpp numpy nose || exit 1
          set
          mkdir build || exit 1
          cd build || exit 1
          echo "Will start the cmake configuration" || exit 1
          cmake .. -DTEST_VIGRANUMPY=1 -DDEPENDENCY_SEARCH_PREFIX="%CONDA_PREFIX%\Library" -DCMAKE_PREFIX_PATH="%CONDA_PREFIX%\Library" || exit 1
          echo "Will start the cmake based build" || exit 1
          cmake --build . --target check --config Release || exit 1
          echo "Completed script successfully" || exit 1

